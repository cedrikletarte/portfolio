# Gmail credentials used by the contact form (src/app/api/contact/route.js)
MAIL_USER=
MAIL_PASS=

# Optional: raises the GitHub API rate limit for the GitHub stats section
# (src/lib/github.ts). Falls back to unauthenticated requests if unset.
GITHUB_TOKEN=
